Зачем нужна файловая система? Мы можем по разному организовывать данные в физическом хранилище(необязательно они должны быть связаны с физическим хранилищем, может быть сеть и тд), в зависимости от цели. Например у нас очень большие потоки данных, и мы хотели бы их сохранить быстро, пренебрегая накладными расходами при последующем поиске и чтении данных.

Кроме того, могут быть разные носители и алгоритмы могут отличаться, появляется необходимость в файловой системе.
Файловая система - это способ организации данных физически. Кроме того нужна программа которая будет реализовывать алгоритмы для организации данных. Получается у файловой системы есть ещё драйвер. 
Что он должен уметь? Он должен уметь работать непосредственно с хранилищем данных и предоставлять общий интерфейс для взаимодействия с файловой системе

Работать с файлами удобно когда они представляют некоторую древовидную структуру. Файловые системы предоставляют возможность выстроить данные в виде дерева. 
У дерева есть корень, обозначается он "/"
Объединить файловые системы можно через точки монтирования, когда мы обращаемся к некоторому файлу, мы проходим по иерархии и если мы ступаем на точку монтирования, то обратимся к соответствующему драйверу файловой системы

![](Pasted%20image%2020250119010204.png)

С точки зрения файловой системы файл это просто набор любых данных
Тип файла - директория, регулярный файл, сокет, пайп и тд, а расширение это для пользователя

В системе хранится mount table, она определяет точки монтирования и для того чтобы управлять содержимым этой таблицы есть системный вызов sys_mount(), утилита mount
![](Pasted%20image%2020250119015705.png)

Ещё есть VFS - Virtual Filew System Switch. VFS представляет собой некоторый набор функций, которые позволяют унифицировать работу с различными файловыми системами
![](Pasted%20image%2020250121163012.png)


На примере снизу видно что когда вызываеттся чтение или запись, сначала смотрим в PageCache в оперативке и если нет подгружаем в пейджкэш а потом подгружаем оттуда в буфер, то есть 2 копирования и это плохо
![](Pasted%20image%2020250124222433.png)

А если мы замаппим эту фигню, то получится что мы будет из адресного пространства сразу отображаться сразу в пейдж кэш
![](Pasted%20image%2020250124224152.png)

ВАЖНО!! ПРО РАЗЛИЧИЕ mmap и FILE API позволяет заммапить регион в пространство с правами на исполнение, чем и пользуются разделяемые библиотеки,
![](Pasted%20image%2020250124224612.png)

Что такое файловый объект для VFS? 

Структура dentry

Структура inode


Вообще файл описывается структурой inode и на него могут указывать несколько dentry с разными именами, так что по сути можно говорить что у файла нет имени

Кто создаёт эти структуры inode и dentry? Их создает файловая система, вообще в файловой системе есть метод lookup, в который передается созданный dentry(даже пустой, для того чтобы можно было закешировать поиск файла даже если его нет) 

Файл символьная ссылка хранит в себе путь до другого файла

Жесткая ссылка это ещё один dentry


Открытие закрытие файла
fd = open("/dir/dir2f", RDWR);
Вообще часто 



Что будет если в картинке снизу будет rm по файлу?
Ответ другие файлы не заметят ничего
![](Pasted%20image%2020250122011609.png)
То есть вообще в иноде хранится счётчик ссылок на него, и пока мы не закроем последний файл и счётчик не обнулится, то lookup() , то мы будем иметь доступ к файлу
То есть как бы open() не открывает файл, а открывает сессию пол работе с файлом



Зачем нам mmap и File API одновременно? Почему нельзя просто ммапить регион в адрессное про

Чем отличается tcp сокет от pipe

Что есть открытие файла? По идее при открытии файла в табличке файловых дескрипторов появляется ссылка на inode