#include <iostream>
#include <string>
#include "HashTable.h"


int main(){
    // Value P1(10,20);
    // Value P2(20, 70);
    // HashTable A;
    // A.Insert("123", P1);
    // A.Insert("456", P1);
    // A.Insert("qwerty", P1);
    // A.Insert("ooo", P1);
    // A.Insert("aaa", P1);
    
    // // A.PrintHashTable();

    // A.Insert("zzzz", P1);
    // A.Erase("zzzz");
    // A.Insert("VVVV", P1);
    // A.Erase("VVVV");
    // A.Insert("OOOO", P1);
    // A.Erase("OOOO");

    // std::cout << "| | | | | | | || | | |" << std::endl;
    // A.PrintHashTable();
    // A.Insert("vvv", P2);
    // A.Insert("ooo", P2);
    // A.Insert("ddd", P2);
    // A.Insert("bbb", P2);
    // A.Erase("123");
    // A.Erase("456");
    // A.Erase("789");
    // A.Erase("qwerty");
    // HashTable B = A;
    // B.Insert("zzzz", P1);
    // B.Erase("zzzz");
    // std::cout << "CAREFUL: " <<(B == A) << " " << B.Size() << " " << A.Size();
    // B.PrintHashTable();


    return 0;
}