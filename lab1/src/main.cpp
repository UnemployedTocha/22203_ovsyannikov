#include <iostream>
#include <string>
#include "HashTable.h"


int main(){
    // Value P1(10,20);
    // HashTable A;
    // A.Insert("123", P1);
    // A.Insert("456", P1);
    // A.Insert("zzz", P1);
    // A.Insert("aaa", P1);
    // A.Insert("aaa1", P1);
    // A.Erase("aaa1");

    // HashTable B = A;
    // B.Insert("zzzz", P1);
    // B.Erase("zzzz");
    // std::cout << "CAREFUL: " <<(B == A) << " " << B.Size() << " " << A.Size();
    //B.PrintHashTable();


    return 0;
}